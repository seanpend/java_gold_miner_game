package mazerunner.engine;

// Interface for interactions in the maze between player and objects
public interface Interactable
{
    public void TriggerInteraction(GameController gameController);
}
